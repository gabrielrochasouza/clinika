import styled from "styled-components";

export const DivContainer = styled.div`
    width: 50px;
    background: var(--grey-p);
    color: var(--tx-p);
`;

export const DivContentSlot = styled.div`
    border-top: 1px solid var(--bg-s);
    height: 30px;
    text-align: center;

    span {
        color: #fff;
    }
`;
